# print("Mean and standard deviation is {} {}".format(population_mean, std_dev))


# fig = ff.create_distplot([data], ["Temp"], show_hist=False)
# fig.show()

# dataset = []
# for i in range(0, 1000):
#     random_index = random.randint(0, len(data))
#     val = data[random_index]
#     dataset.append(val)
# newDS = []
# for i in range(0, 100):
#     rand_index = random.randint(0, 1000)
#     rval = dataset[rand_index]
#     newDS.append(rval)

# sample_mean = statistics.mean(dataset)
# sample_stddev = statistics.stdev(dataset)
# print("Sample mean is {} and sample standard deviation is {}".format(sample_mean, sample_stddev))

# fig = ff.create_distplot([newDS], ["Temp"], show_hist=False)
# fig.show()